<!--
Template: AC_template.md (Acceptance Criteria Template)
Usage: Replace all {{placeholders}} with actual content.
       Read ALL <!-- ИНСТРУКЦИЯ: --> comments — they contain mandatory instructions.
       Remove sections marked <!-- ОПЦИОНАЛЬНО: --> if not applicable.
-->

# Описание

<!-- ИНСТРУКЦИЯ: Укажите статус User Story.
ВАЛЮТЫ: draft, in_progress, verified, done
Формат: статус на отдельной строке без маркирования. -->
status: {{Статус User Story (draft / in_progress / verified / done)}}

|   |   |
| --- | --- |
| **Как** | {{Роль пользователя или системы}} |
| **Я хочу** | {{Что пользователь/система хочет делать}} |
| **Чтобы** | {{Зачем — ценность или бизнес-цель}} |

<!-- ИНСТРУКЦИЯ: Заполните таблицу User Story в формате «Как — Я хочу — Чтобы».
- «Как» — укажите роль (Конечный пользователь: «Налоговый инспектор (НИ)», «Заявитель» ИЛИ Система: «Система (ваш продукт)»).
- «Я хочу» —формулируйте действие, которое должна выполнять система или пользователь.
- «Чтобы» — опишите бизнес-ценность (почему это нужно, какой результат ожидается).
Ожидаемый результат: чёткое однозначное описание需求的, помещающееся в 1–2 предложения на строку. -->

## Критерии приемки

<!-- ИНСТРУКЦИЯ: Ниже приведены все возможные секции критериев приемки.
ОСТАВЬТЕ только те секции, которые применимы к вашей User Story.
Удалите остальные секции целиком (включая заголовок) — не оставляйте пустых. -->

<!-- ИНСТРУКЦИЯ: Секция «Условия и триггеры».
Опишите, при каких условиях и по какому событию запускается процесс/действие.
Примеры: «при присвоении статуса X», «при поступлении сообщения из Y», «после прохождения Z проверок».
Если User Story не имеет явных триггеров — удалите эту секцию. -->
### {{Название секции: Условия и триггеры (или аналогичное)}}

1. {{Условие 1 — при каком событии или состоянии инициируется действие}}
2. {{Условие 2 — дополнительные предусловия или ограничения}}

<!-- ИНСТРУКЦИЯ: Секция «Содержание формируемого документа/сообщения».
Опишите, какие данные/поля/реквизиты содержатся в выходном артефакте (квитанция, уведомление, сообщение и т.п.).
Формат: нумерованный список, каждый пункт — один обязательный элемент.
Удалите эту секцию, если User Story не формирует документ. -->
### {{Название секции: Содержание формируемого документа}}

1. {{Документ/сообщение содержит обязательный реквизит 1}}
2. {{Документ/сообщение содержит обязательный реквизит 2}}
3. {{Документ/сообщение содержит обязательный реквизит 3}}

<!-- ИНСТРУКЦИЯ: Секция «Каналы доставки».
Опишите канал(ы), через которые результат доставляется получателю.
Укажите:
- Конкретные каналы (ГП-3, ЭДО, Kafka, API и т.п.).
- Механизм выбора канала (если зависит от источника).
- Требования к подписанию/формату.
Удалите эту секцию, если доставка не является частью User Story. -->
### {{Название секции: Каналы доставки (или «Отправка X»)}}

1. {{Канал доставки 1 — описание и условие использования}}
2. {{Канал доставки 2 — описание и условие использования}}
3. {{Требования к формату / подписанию / подтверждению доставки}}

<!-- ИНСТРУКЦИЯ: Секция «Хранение и доступность».
Опишите требования к сохранению результата в базе данных:
- Срок хранения (укажите в годах).
- Связь с родительским объектом.
- Возможность повторной выгрузки/доступа.
Удалите эту секцию, если хранение не применяется. -->
### {{Название секции: Хранение и доступность}}

1. Система сохраняет копию {{объекта}} в базе данных.
2. {{Объект}} связан с соответствующим {{родительский объект}}.
3. {{Объект}} доступен для повторной выгрузки в любое время.
4. Система сохраняет {{объект}} в течение {{срок хранения}} лет.

<!-- ИНСТРУКЦИЯ: Секция «Обработка результатов по кодам/статусам».
Применяется, если User Story обрабатывает входящие данные с разными кодами/типами результатов.
Опишите каждый тип результата как подсекцию `####` с:
- Присваиваемым статусом (код + наименование).
- Переходами статусов (из → в).
- Сохраняемыми деталями.
Удалите эту секцию, если обработка по кодам не применяется. -->
### {{Название секции: Обработка результатов}}

<!-- ИНСТРУКЦИЯ: Для каждого результата/кода создайте подсекцию `####`.
Скопируйте эту структуру для каждого типа результата. -->
#### {{Подзаголовок: название результата (например, «ДОПУСК ТС» / «IMPORTED»)}}

1. Система {{действие — присваивает статус «{{статус код}}» ({{статус наименование}}).}}
2. Переходы статусов:
   1. «{{Старый статус 1}}» → «{{Новый статус}}»
   2. «{{Старый статус 2}}» → «{{Новый статус}}»
3. Система сохраняет {{детали, которые сохраняются}}.
4. Переход статуса фиксируется в истории изменений {{объекта}}.

<!-- ИНСТРУКЦИЯ: Секция «Логика обработки» (общая).
Опишите ключевые правила, алгоритмы или бизнес-логику:
- Место в бизнес-процессе (этап, позиция в цепочке проверок).
- Используемые справочники/словари/компоненты.
- Правила анализа (морфология, контекст и т.п.).
- Исключения и edge-случаи.
Эта секция гибкая — адаптируйте заголовок и содержание под конкретную US.
Удалите, если логика обработки не требует отдельного описания. -->
### {{Название секции: бизнес-логика или правило обработки}}

1. {{Правило или алгоритм 1}}
2. {{Правило или алгоритм 2}}

<!-- ИНСТРУКЦИЯ: Секция «Обработка ошибок».
Опишите поведение системы при ошибках:
- Валидация входящих данных.
- Повторные попытки (количество, интервал).
- Действия при исчерпании попыток.
- Уведомление администратора.
Присутствует в большинстве AC — обычно ОСТАВЛЯЕТся. -->
### {{Название секции: Обработка ошибок}}

1. {{При какой ошибке система реагирует}}
2. При ошибке система автоматически повторяет попытку (до {{число}} раз).
3. При невозможности {{действие}} система фиксирует ошибку в журнале событий.
4. Система уведомляет администратора о критических проблемах.

<!-- ИНСТРУКЦИЯ: Секция «Логирование и аудит».
Опирайтесь на эту секцию почти всегда. Укажите:
- Что логируется (список полей/элементов).
- Формат логирования (маскирование чувствительных данных, если применимо).
- Срок хранения логов.
Удалите только если логирование не требуется. -->
### Логирование{{_и_аудит_опционально}}

1. Система логирует все операции по {{наименование процесса}}:
   1. {{Параметр логирования 1}}
   2. {{Параметр логирования 2}}
   3. {{Параметр логирования 3}}
   4. {{Параметр логирования 4}}
   5. {{Параметр логирования 5}}
2. {{Дополнительное требование к логированию (маскирование, формат, частота)}}
3. Система сохраняет логи в течение {{срок хранения}} лет.

<!-- ОПЦИОНАЛЬНО: Секция «Распаковка/детальный алгоритм».
Применяется, если User Story содержит пошаговый технический алгоритм (работа с файлами, очередями, API).
Удалите, если пошаговый алгоритм не требуется. -->
### {{Название секции: детальный алгоритм}}

1. {{Компонент}} выполняет {{шаг 1}}:
   1. {{Подшаг 1.1}}
   2. {{Подшаг 1.2}}
2. {{Компонент}} выполняет {{шаг 2}}
3. {{Компонент}} выполняет {{шаг 3}}

<!-- ОПЦИОНАЛЬНО: Секция «Взаимосвязь с другими US».
Применяется, если User Story имеет чёткие вызывающие/вызываемые зависимости от других US.
Удалите, если зависимости не требуется выделять. -->
## Взаимосвязь с другими US

- **{{US-X.Y.Z}}**: {{Описание взаимосвязи — например, «Вызывается после...» или «Используется для...»}}
- **{{US-A.B.C}}**: {{Описание взаимосвязи}}

<!-- ИНСТРУКЦИЯ: Секция «Связанные требования».
Перечислите связанные требования из ТЗ (REQ-XXX) и/или связанные US.
Формат: маркированный список, одна ссылка на строку.
Удалите секцию, если связанных требований нет. -->
## Связанные требования

- {{Ссылка на требование или US с кратким описанием}}
- {{Ссылка на требование или US с кратким описанием}}
