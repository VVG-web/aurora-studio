# Быстрый старт — маршруты рутинной работы.
#
# Маршрут проходится одной кнопкой: панель выполняет шаги по порядку и останавливается,
# только если команда не отработала (код 2 и выше). Код 1 — «отработала и нашла, что
# чинить» — маршрут не останавливает, это нормальное состояние живой базы.
#
# Формат:
#   [id] Название маршрута | когда его проходят | вкладка
#   команда | зачем этот шаг | флаги (необязательно)
#   - произвольный текст | что делает человек | что сказать ассистенту (необязательно)
#   цикл: | зачем повторять — начало блока, который идёт партиями
#   конец цикла | — конец блока
#
# Цикл — это про то, что прогон можно выключить в любой момент. Шаги между «цикл:» и
# «конец цикла» повторяются партиями: разобрали пятнадцать источников, осмыслили их,
# связали, посчитали доверие, закоммитили — и снова. Раньше это были фазы по всей базе,
# и остановка на середине оставляла карточки без типов, тезисов и связей: сотни ошибок и
# ноль пригодного знания. Теперь каждый оборот добавляет готовое.
#
# Флаги пишутся так, как маршрут должен пройти начисто: с `--apply` там, где шаг обязан
# записать результат. Кнопка «Посмотреть» снимает `--apply` со всех шагов и показывает
# тот же маршрут без записи.
#
# `--allow-dirty` у пишущих шагов не прихоть: после первого же шага дерево перестаёт быть
# чистым, и без него маршрут остановился бы на втором. Откат при этом не теряется —
# зафиксируйте свою работу до маршрута, и `git reset --hard` вернёт всё, что он сделал.
#
# Шаг-человек (строка с «-») панель не запускает: это работа, которую машина не делает.
# Таких шагов осталось мало — то, что раньше уходило в чужой чат, теперь делает
# встроенный агент командами `agent:*`.
#
# Вкладка — «база» (обслуживание, по умолчанию) или «продуктивность» (артефакты, ревью,
# поставка). Разные занятия и разные дни: в одном списке нужное приходится искать среди
# чужого.
#
# Файл лежит в ките и правится как обычный текст: добавили строку — появился шаг.

[update] Обновить базу | взять новое из источников и довести до знания: синхронизация, разбор партиями, тезисы, связи, доверие. Маршрут идёт, пока есть работа — хоть двенадцать часов, — и фиксирует каждый оборот в git: выключайте когда угодно, сделанное останется и переделывать его не придётся. В итоге: база знает всё, что появилось в источниках | база
sync:confluence | Забрать свежие страницы Confluence
sync:jira | Забрать свежие задачи Jira
sync:audit | Убедиться, что зеркала целы: строить будем из них
kb:ingest-office | Превратить docx/pdf/xlsx из Raw/ в текст, который движок умеет читать
kb:build | Вернуть в план источники, которые изменились после разбора | --reopen --apply
цикл: | Партия за партией: каждый оборот добавляет готовое знание, а не заготовки
agent:build | Разобрать очередную партию источников на карточки | --apply --critic
kb:kind | Проставить тип новым карточкам: словарь, документ, знание | --apply
agent:distill | Написать тезисы для карточек-знаний, Момус проверяет каждую | --apply
kb:links | Связать новые карточки с базой | --cards --apply --allow-dirty
kb:moc | Карта на каждый разобранный документ: откуда пришла карточка | --by-source --apply --allow-dirty
конец цикла | Источники кончились — дальше то, что делается один раз по всей базе
kb:repair | Починить ссылки, которые появились по ходу разбора | --links --apply --allow-dirty
agent:aliases | Развести синонимы: где карточки разные — уточнить, где двойники — человеку | --apply --critic
kb:moc | Карты содержания по группировкам: карточек без связей не бывает | --apply --allow-dirty
kb:index | Пересобрать оглавления разделов | --apply
kb:correct | Наложить исправления человека: разбор переписал тела карточек, а знание человека сильнее источника | --apply
ops:trace-table | Таблица связей «артефакт ↔ задача»: на ней считается доверие | --apply
kb:trust | Пересчитать доверие по статусам задач | --apply
kb:embed | Обновить семантический индекс для поиска по смыслу | --apply
kb:lint | Что осталось механически неисправным
ops:stats | Итог: состав базы, доверие, динамика

[fix] Починить базу | привести в порядок то, что уже есть: битые ссылки, двойники, карты, оглавления, схема. В источники не ходит — только внутрь базы. В итоге: база связна, дублей нет, всё найдётся и по ссылке, и по карте | база
kb:repair | Починить битые ссылки, гомоглифы и легаси-поля | --links --homoglyphs --frontmatter --apply --allow-dirty
kb:dedupe | Найти двойников и слить бесспорных | --merge-all --apply --allow-dirty
agent:aliases | Спорные синонимы: модель уточняет там, где карточки разные | --apply --critic
kb:repair | Завести заготовки под ссылки, которым не на что указывать | --stubs --apply --allow-dirty
kb:moc | Карта на каждый разобранный документ | --by-source --apply --allow-dirty
kb:moc | Карты содержания: до каждой карточки должно быть можно дойти | --apply --allow-dirty
kb:index | Пересобрать оглавления разделов | --apply
kb:schema | Проверить схему карточек и мигрировать отставшие
kb:build | Что разобрано слишком тонко и стоит перечитать | --thin
kb:map | Ревизия качества: пары, говорящие об одном, карточки размером с документ, острова и мосты
kb:embed | Обновить семантический индекс | --apply
ops:search-quality | Качество поиска числом: находит ли база сама себя | --golden --apply
kb:lint | Что осталось неисправным после починки
ops:todo | Что осталось лично вам — с объяснением, почему это не кнопка

[rebuild] Пересобрать базу с нуля | опасное и редкое: снести всё, что соберётся заново, и собрать по текущим правилам. Карточки без источника и без метки машины остаются. Прогон нарезан циклами. В итоге: база собрана заново целиком | база
sync:audit | Убедиться, что зеркала целы: строить будем из них
kb:reset | Снести то, что соберётся заново; неопознанное остаётся | --apply
цикл: | Партия за партией, это часы работы — запускайте на ночь. Выключите в любой момент: разобранное останется годным
agent:build | Разобрать очередную партию источников | --apply --critic
kb:kind | Проставить тип карточкам партии | --apply
agent:distill | Написать тезисы, Момус проверяет каждую | --apply
kb:links | Связать карточки партии | --cards --apply --allow-dirty
kb:moc | Карта на каждый разобранный документ | --by-source --apply --allow-dirty
конец цикла | Источники кончились — дальше по всей базе
kb:moc | Карты содержания по группировкам | --apply --allow-dirty
kb:index | Оглавления разделов | --apply
kb:correct | Наложить исправления человека: сборка переписала тела карточек | --apply
ops:trace-table | Таблица связей «артефакт ↔ задача» | --apply
kb:trust | Доверие по статусам задач | --apply
kit:remap-sources | Вернуть артефактам основания: имена карточек сменились | --apply
kb:embed | Семантический индекс | --apply
ops:stats | Итог: что получилось
ops:todo | Что осталось лично вам

[write] Написать артефакт | новая история, AC или спецификация — от вопроса к базе до готового файла по шаблону проекта | продуктивность
agent:ask | Спросить базу по теме: что она уже знает и чего ей не хватает | --mode generate
ctx:context | Собрать пак знаний под задачу и сохранить его | --save
make:create | Создать артефакт по шаблону проекта
- Написать содержание | ассистент пишет по паку и шаблону, вы правите | /aurora-vault make:create
make:review | Проверить артефакт против базы: расхождения и пробелы
- Сверить основания | посмотрите `based_on` артефакта: на каких карточках он стоит и что придётся пересматривать, если знание изменится | /aurora-vault ops:impact --explain

[deliver] Сдать документы | собрать поставляемое из базы, проверить, выгрузить и заморозить переданную версию | продуктивность
ops:trace | Трассировка: пункт ТЗ → требование → спецификация → задача → приёмка
make:assemble | Собрать поставляемый документ из базы по шаблону
make:review | Проверить собранное против базы
ship:export | Выгрузить в docx/pdf по фирменному шаблону
ship:publish | Опубликовать артефакт в Confluence как generated-страницу
ship:release | Заморозить переданную версию: снимок, коммит, дата
